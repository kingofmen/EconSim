#include "population/production_evaluator.h"

namespace population {

double LocalProfitMaximiser::Evaluate(const ProductionContext& context,
                                      geography::proto::Field* target) const {
  return 0;
}

}  // namespace population
